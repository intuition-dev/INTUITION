doctype html
html
    head
        include headFrag

        script.
            window.ROOT = '#{ROOT}';

        script.
            window.apiPort = location.port
            window.apiProtocol = location.protocol.split(':')[0];
            window.apiHost = location.hostname;

            window.appMount = 'http://0.0.0.0:'+location.port;
            window.siteName = 'MetaBake';

        link(rel='stylesheet', href= ROOT +'editors/assets/css/spectreTop.css')

    body
        script(src=ROOT +'editors/assets/js/loader.js')

        block header
        block content