div.py-2
   h4.text-center Donut Chart
   div#c3-chart-donut
   div.mb-2
   h4.text-center TimeSeries Chart
   div#c3-chart-timeseries

   script.
      depp.require(['c3'], function(){
         config = {};
         configTimeseries = {};
      });

      depp.require(['c3', 'loadModel', 'loadC3Bind'], setupChartView);

      function setupChartView(){
         let canvasId = 'c3-chart-donut';
         let canvasId2 = 'c3-chart-timeseries';
         let chart  = new C3Bind();
         chart.init(canvasId);
         chart.init2(canvasId2);
      }