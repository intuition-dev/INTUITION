div.py-2
   h4.text-center Balloons Chart
   canvas#chartjs-chart-balloons

   script.
      depp.require(['chartjs'], function(){
         config = {}
      });

      depp.require(['loadModel', 'loadChartjsBind'], setupChartView);

      function setupChartView(){
         let canvasId = 'chartjs-chart-balloons';
         let chart  = new ChartjsBind();
         chart.init2(canvasId);
      }