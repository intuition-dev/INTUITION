extends /layout/layout

block head2
   //- App Shell
   //- in head, $$$ get for speed
   script(src=ROOT+ 'news/tag/table-comp.js')
   script.
      axios.get('/news/list.json').then(function(resp) {
         news = resp.data
         console.info('n ready')
         //loadjs.done('news')
      })// fetch

//- block top
//-    style.
//-       .pageBack {
//-          background-image: url(img.jpg);
//-       }

//-    .pageBack.delayShowing(data-js-menu-highlight="news")
//-       include /layout/nav
//-       br
//-       .pad
//-          H1.flex.white.fLarge.fBod The ACME Corporation


block main
   .pad
      H1.cAccent.line.fBod Press Releases
      table-comp

   script.
      const tag = riot.mount('table-comp')[0] // mount the first tag
      //loadjs.ready('news', function() {
            tag.render(news)
      //})

   //-link(href= ROOT + 'assets/css/debug.css', rel='stylesheet')

